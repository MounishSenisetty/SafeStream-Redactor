# SafeStream-Redactor
Streaming PII &amp; credential redaction for massive text files — constant O(1) memory, multi-tier detection (regex + NER + contextual heuristics), fully customizable redaction.
